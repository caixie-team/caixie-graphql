{
  "name": "@cxql/batch-plugin",
  "version": "0.1.0",
  "description": "",
  "scripts": {
    "watch": "tsc -p ./tsconfig.build.json",
    "build": "rimraf lib && tsc -p ./tsconfig.build.json",
    "lint": "tslint --fix --project ./",
    "test": "jest --config ./jest.config.js",
    "ci": "pnpm build"
  },
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "files": [
    "lib/**/*"
  ],
  "devDependencies": {
    "@cxql/core": "workspace:*",
    "@cxql/shared": "workspace:*",
    "graphql": "^16.6.0",
    "rimraf": "^3.0.2"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
